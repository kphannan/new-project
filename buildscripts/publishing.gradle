// --- Artifact Publishing
publishing {
    publications {
        maven( MavenPublication ) {
            groupId = 'com.example'
            artifactId = 'default-application'
            version = '0.0'
        }
    }
}
